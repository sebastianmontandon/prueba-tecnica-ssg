# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/crm_dashboard

# Example for local development:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/crm_dashboard

# PostgreSQL Connection Details (alternative format)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crm_dashboard
DB_USER=postgres
DB_PASSWORD=password